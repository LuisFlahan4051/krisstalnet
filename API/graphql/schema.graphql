type PorHacer {
  id: ID!
  text: String!
  done: Boolean!
  user: User!
}

type User {
  id: ID!
  name: String!
}

type Query {
  todos: [PorHacer!]!
}

input NewPorHacer {
  text: String!
  userId: String!
}

type Mutation {
  createPorHacer(input: NewPorHacer!): PorHacer!
}